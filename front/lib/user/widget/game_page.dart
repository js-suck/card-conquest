import 'package:flutter/material.dart';

class GamesPageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Games Page', style: TextStyle(fontSize: 24)),
    );
  }
}
