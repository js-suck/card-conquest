import 'package:flutter/material.dart';

class TournamentsPageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tournaments Page', style: TextStyle(fontSize: 24)),
    );
  }
}
